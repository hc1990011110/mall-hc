spring:
  profiles:
    active: dev
  application:
    name: mall-backend
dubbo:
  application:
    name: mall-backend-dubbo
  registry:
    address: nacos://localhost:8849
  provider:
    timeout: 3000
  consumer:
    timeout: 3000
    check: false